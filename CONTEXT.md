# Pilot Guardian

Pilot Guardian 在网页访问发生前施加有意的摩擦，让使用者按照自己设定的条件访问容易分心的网站。

## Language

**URL 匹配模式（URL Pattern）**：
一条以协议、主机和路径通配符描述网页地址范围的字符串。
_Avoid_: URL 正则、网址

**受控目标（Protected Target）**：
同一访问规则下共享访问策略与运行状态的一组 URL 匹配模式。
_Avoid_: 网站、网页

**访问规则（Access Rule）**：
一个受控目标与一项访问策略的有优先级组合；同一地址只采用最优先的匹配规则。
_Avoid_: 拦截项、网站配置

**访问策略（Access Policy）**：
决定一次受控访问当前应当放行、等待还是接受挑战的条件。
_Avoid_: 模式、限制逻辑

**闸门（Gate）**：
在访问尚未获准时替代受控目标展示的交互页面。
_Avoid_: 页面 A、拦截页

**挑战步骤（Challenge Step）**：
闸门中按顺序完成的一项验证，可以是文本挑战或交互挑战。
_Avoid_: 密码框、题目、自定义口令

**文本挑战（Text Challenge）**：
通过输入与预设答案一致的文本完成的挑战步骤。
_Avoid_: 普通口令、密码挑战

**交互挑战（Interactive Challenge）**：
通过完成自定义互动体验而非提交预设文本完成的挑战步骤。
_Avoid_: 自定义口令、HTML 口令

**挑战序列（Challenge Sequence）**：
一条访问规则中需要按既定顺序完成的全部挑战步骤。
_Avoid_: 题目列表、口令列表

**挑战场景（Challenge Scene）**：
可选地附属于单个挑战步骤的可定制体验；文本挑战将其作为口令卡片背景，交互挑战则将其作为完整互动内容。未指定时使用默认场景。
_Avoid_: 自定义页面、背景 HTML

**官方模板（Official Template）**：
仅供交互挑战使用、由扩展内置且具有稳定标识和可调参数的挑战场景，可复制为独立的自定义场景继续编辑。文本挑战不引用官方模板。
_Avoid_: 默认 HTML、示例代码

**放行窗口（Access Window）**：
一次授权持续有效、受控目标可直接访问的连续时间段。
_Avoid_: 会话、解锁状态

**冷却期（Cooldown）**：
间隔策略在上一个放行窗口结束后、下一次允许开启窗口前的等待时间。
_Avoid_: 封禁、倒计时

**开放日（Open Day）**：
每周或每月策略指定的、按本地自然日计算的可访问日期。
_Avoid_: 白名单日期

**开放时段（Open Hours）**：
访问规则可选的日内限制；闸门只在开放时段内放行或接受挑战，已开启的放行窗口不因时段结束而收回。
_Avoid_: 时间限制、宵禁、可访问时间
