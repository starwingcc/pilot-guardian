# Pilot Guardian 开发纲领

## 产品定位

Pilot Guardian 是一个基于 Chrome Manifest V3 的网页访问控制扩展。它在受控目标加载前引导用户进入闸门，并依据访问策略决定放行、等待或要求完成挑战。

产品的核心目标是增加有意识的访问摩擦，而不是提供企业级网络封锁、家长控制或安全边界。普通顶层导航是主要保障范围，SPA 导航由内容脚本补偿；浏览器内部页面、扩展商店、子资源请求和下载不属于控制范围。

## 领域语言

开发和界面文案应沿用 `CONTEXT.md` 中定义的统一术语，包括受控目标、访问规则、访问策略、闸门、挑战步骤、放行窗口、冷却期和开放日。新增概念或改变既有语义时，先更新领域文档；影响长期架构的决策应记录为 ADR。

## 技术基线

- WXT 负责扩展工程、入口组织和构建。
- React 与 TypeScript 用于界面和应用逻辑。
- Chrome Manifest V3 API 承担导航控制、状态存储和扩展生命周期管理。
- shadcn/ui 作为通用组件基础，Radix 提供交互原语，Lucide 是唯一图标来源。
- Tailwind CSS v4 提供设计令牌和工具类，SCSS 承载全局语义样式、复用能力与页面级复杂视觉效果。
- 配置和运行状态保持本地优先，不依赖远程服务。

## 项目架构

### 扩展入口

`entrypoints/` 按浏览器运行环境划分职责：后台入口负责策略执行和浏览器 API 协调；内容脚本负责感知页面内导航；闸门负责挑战与等待体验；设置页负责完整配置；弹窗负责轻量状态查看和快捷入口。

入口层只组合能力，不应成为领域规则的定义位置。多个入口共享的行为应下沉到 `src/`。

### 领域层

`src/domain/` 是业务规则的唯一事实来源，包含领域类型、默认值、输入校验、地址匹配和访问策略计算。领域层应保持纯净、可预测且可独立测试，不依赖 React、DOM 或 Chrome API。

配置格式以当前 schema 为准。不要在没有明确产品需求的情况下增加旧版本兼容、隐式迁移或降级导出。

### 运行时层

`src/runtime/` 负责把领域决策连接到 Chrome API，包括消息协议、存储、导航控制和动态网络规则。此层负责权限与生命周期边界，但不重复实现领域策略。

闸门和其他 UI 只能通过明确的运行时消息获取必要信息。挑战答案等敏感字段不得暴露给不需要它们的页面上下文。

### 界面层

`src/components/ui/` 保存 shadcn/ui 基础组件；页面专属组合留在对应入口目录。优先组合现有基础组件，只有稳定且跨页面复用的模式才提升为共享组件。

所有图标统一使用 Lucide，界面中禁止使用表情符号或字符模拟图标。视觉设计应保持鲜明、沉浸且统一，同时优先保证可访问性、键盘操作、清晰反馈和 Reduced Motion 支持。

### 样式层

`src/styles/tailwind.css` 是 Tailwind v4 的纯 CSS 入口；`src/styles/globals.scss` 保存跨页面语义变量与基础规则；`src/styles/_mixins.scss` 保存真正复用的 SCSS 能力；每个入口的 `style.scss` 负责页面视觉与布局。

避免把页面专属样式提升为全局规则，也避免为了使用 SCSS 而制造无意义嵌套。设计令牌和组件变体应优先于零散的魔法值。

## 数据与安全边界

配置、挑战答案和运行状态存储在浏览器本地。挑战答案当前是明文数据，导出文件也包含明文答案；任何界面或文档都不得暗示其经过加密或具备强安全保证。

请求新权限时遵循最小权限原则，并说明用户价值与风险。不得静默引入遥测、远程同步、第三方账号或网络上传能力。

## 开发原则

- 保持领域逻辑与浏览器副作用分离。
- 保持配置校验、运行时决策和界面展示对同一业务语义的一致理解。
- 优先进行小而完整的改动，避免无关重构和未经请求的兼容层。
- 尊重工作区中的既有修改，不覆盖或清理来源不明的文件。
- 架构或行为发生变化时同步维护相关文档和测试。
- 不为未来可能出现的需求提前增加抽象；共享边界稳定后再提取。
- 当前处于开发阶段，配置格式、存储结构和 API 可以随时打破兼容；不要在没有明确产品需求时增加旧版本迁移代码、降级路径或向后兼容层。

## 验证策略

根据改动风险运行最小充分验证。领域逻辑应以单元测试覆盖，类型与构建配置变更应进行静态检查，扩展打包相关变更应验证生产构建。

除非用户在当前请求中主动提及或明确要求，否则不得自动运行端到端测试。不得因界面、导航或扩展配置发生变化而自行推断用户授权了端到端测试。

只报告实际运行过的检查，不得把未执行的验证描述为通过。
